import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.uets.pokedex',
  appName: 'Pokedex',
  webDir: 'dist/pokedex/browser'
};

export default config;
